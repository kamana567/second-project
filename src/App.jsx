import "./App.css";



import Navbar, { Fullname } from "./components/navbar";
import New from "./components/newcard";

const App = () => {
  // const city = "butwal";
  // const country = "nepal";

  return (
    <div>
       {/* <h1> */}
        {/* {country === "nepal" */}
          {/* ? "this is a nepal address." */}
          {/* : country === "india" */}
          {/* ? "this is india address." */}
          {/* : country === "china" */}
          {/* ? "this is china address." */}
          {/* : "country not recognized."} */}
      {/* </h1> */}
      {/* <Address city={city} country={country} /> */}

    
      
      <New/>
    </div>
  );
};
export default App;
